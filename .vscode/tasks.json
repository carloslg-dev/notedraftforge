{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build:angular",
      "type": "shell",
      "command": "bash",
      "args": [
        "-lc",
        "export NVM_DIR=\"${NVM_DIR:-$HOME/.nvm}\" && source \"$NVM_DIR/nvm.sh\" && nvm use 20 >/dev/null && npm run build"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": "$tsc"
    },
    {
      "label": "serve: angular",
      "type": "shell",
      "command": "bash",
      "args": [
        "-lc",
        "export NVM_DIR=\"${NVM_DIR:-$HOME/.nvm}\" && source \"$NVM_DIR/nvm.sh\" && nvm use 20 >/dev/null && npm run start"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": "none",
      "problemMatcher": "$tsc-watch",
      "isBackground": true
    }
  ]
}
