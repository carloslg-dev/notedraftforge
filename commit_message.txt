fix: work-view navigation build and workflows

Closes #20

Summary of changes:
- Verified the E2E React Router navigation from Work List to Work View (using the already implemented mock views from previous PRs).
- Fixed a TS compilation error in `get-piece.use-case.spec.ts` where `vi.Mocked` was used incorrectly.
- Completed required AI workflow documentation (`.ai/current-task.md` and `openspec/changes/20-work-view-navigation/execution-record.md`).

Checklist of Acceptance Criteria:
- [x] Tapping list item navigates to /work/:pieceId (Verified via Playwright script locally)
- [x] Work view loads the correct piece (Verified via Playwright script, mock piece loads)
- [x] Back navigation returns to Work List (Verified via Playwright script)
- [x] Respects domain invariants (No domain changes needed, routing is in UI shell)
- [x] Follows hexagonal architecture (Routing handled exclusively in React components)
- [x] No business logic inside adapters (N/A, UI change only)

Ambiguities Found:
- Most of the navigation and shell setup was already completed by PR #69. The work here focused on confirming the requirements, fixing a stray build issue blocking CI, and formalizing the completion in the AI workflow system.
